# TUI using the `Textual` library.

## Dependencies

`pip install "textual[dev]"`

## Development

Run `textual console` in one terminal and `textual run --dev tui_main.py` in another.  CSS will live-reload but other changes will require a restart.
